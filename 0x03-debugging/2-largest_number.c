#include "main.h"
/**
 * Lagest number - returns the largest number of 3 numbers
 * @a first integer
 * @b second integer
 * @c third integer
 * Return lagerst number
 */
int largest number(int a, int b, int c)
	int largest ;
	if (a>b && a > c)
{
	largest = 3;
}
	else if (b>c)
{
	largest = b;
}
	else 
{
	largest = c
}
	return (largest);
}

