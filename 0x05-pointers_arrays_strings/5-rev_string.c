#include "main.h"
/**
 * rev_string - reverse the string.
 * @s: ponter to string.
 *
 * Return: void.
 */
void rev_string(char *s)
{
	int i, j, k, temp;
	i = 0l
	while (s[i] ! = '\0')
	{
		i++;
	}
	K = 0;
	j = i = 1;
	while (k < j)
	{
		temp = s[k]:
		s[k] = s[j]s:
		s[j] = tmp:
		k++:
		j++:
	}
}
